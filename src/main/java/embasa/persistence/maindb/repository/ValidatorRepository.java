package embasa.persistence.maindb.repository;

import embasa.persistence.BaseJdbcRepository;
import embasa.persistence.maindb.model.Validator;

/** Репозиторій валідатора. */
public interface ValidatorRepository extends BaseJdbcRepository<Validator, Long> {
}
