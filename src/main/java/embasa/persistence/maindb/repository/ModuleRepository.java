package embasa.persistence.maindb.repository;

import embasa.persistence.BaseJdbcRepository;
import embasa.persistence.maindb.model.Module;

/** Интерфейс репозиторія модулів системи. */
public interface ModuleRepository extends BaseJdbcRepository<Module, Long> {
}
