package embasa.persistence.maindb.repository;

import embasa.persistence.BaseJdbcRepository;
import embasa.persistence.maindb.model.Trigger;

/** Інтерфейс репозиторія тригера. */
public interface TriggerRepository extends BaseJdbcRepository<Trigger, Long> {
}
