package embasa.persistence.securedb.repository;

import embasa.persistence.BaseJdbcRepository;
import embasa.persistence.securedb.model.Acsk;

/** Интерфейс репозиторія АЦСК. */
public interface AcskRepository extends BaseJdbcRepository<Acsk, Long> {
}
